import { ToolbarTitleService } from './toolbar-title.service';

describe('ToolbarTitleService', () => {
  it('should create an instance', () => {
    expect(new ToolbarTitleService()).toBeTruthy();
  });
});
