import { SubscriptionsService } from './subscriptions.service';

describe('SubscriptionsService', () => {
  it('should create an instance', () => {
    expect(new SubscriptionsService()).toBeTruthy();
  });
});
